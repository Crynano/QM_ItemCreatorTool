<UserControl x:Class="QM_ItemCreatorTool.View.Tabs.GeneralTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QM_ItemCreatorTool.View.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Background="LightBlue">
        <DockPanel LastChildFill="False" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center">
            <DockPanel DockPanel.Dock="Top">
                <TextBlock Style="{StaticResource TextBlockHeader2}" DockPanel.Dock="Top" Margin="5,5,5,0" Text="Load Mod Info:" HorizontalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Top" Margin="5" Text="{Binding LoadModInfo}" TextWrapping="Wrap" TextAlignment="Center"/>
                <Button Style="{StaticResource ImportantButton}" Content="Load Mod" DockPanel.Dock="Top" Margin="5" Command="{Binding LoadModCommand}"/>
            </DockPanel>
            <DockPanel DockPanel.Dock="Top">
                <TextBlock Style="{StaticResource TextBlockHeader2}" DockPanel.Dock="Top" Margin="5,5,5,0" Text="Create Mod Info:" HorizontalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Top" Margin="5" Text="{Binding CreateModInfo}" TextWrapping="Wrap" TextAlignment="Center"/>
                <Button Style="{StaticResource ImportantButton}" Content="Create Mod" DockPanel.Dock="Top" Margin="5" Command="{Binding CreateModCommand}"/>
            </DockPanel>
            <Separator DockPanel.Dock="Top"></Separator>
            <DockPanel DockPanel.Dock="Top">
                <TextBlock Style="{StaticResource TextBlockHeader2}" DockPanel.Dock="Top" Margin="5,5,5,0" Text="Clear Mod Data Info:" HorizontalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Top" Margin="5" Text="{Binding ClearModDataInfo}" TextWrapping="Wrap" TextAlignment="Center"/>
                <Button Style="{StaticResource ImportantButton}" Background="IndianRed" Content="Clear Current Mod Data" DockPanel.Dock="Top" Margin="5" Command="{Binding ClearModCommand}"/>
            </DockPanel>
        </DockPanel>
    </DockPanel>
</UserControl>
