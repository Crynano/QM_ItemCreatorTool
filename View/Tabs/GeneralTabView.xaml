<UserControl x:Class="QM_ItemCreatorTool.View.Tabs.GeneralTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QM_ItemCreatorTool.View.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <DockPanel LastChildFill="False" Margin="5">
                <TextBlock Text="Config File Root or File" DockPanel.Dock="Top" Margin="5"></TextBlock>
                <DockPanel DockPanel.Dock="Top" Margin="5,0,5,5">
                    <Button Content="Explore Path" DockPanel.Dock="Right"
                            Command="{Binding SelectRootFolderCommand}"></Button>
                    <TextBox DockPanel.Dock="Bottom" Text="{Binding Path=ConfigFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                </DockPanel>
                <Button Content="Load Mod" DockPanel.Dock="Top" Margin="5" Height="50" Command="{Binding LoadModCommand}"/>
                <Button Content="Create Mod" DockPanel.Dock="Top" Margin="5"/>
            </DockPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
