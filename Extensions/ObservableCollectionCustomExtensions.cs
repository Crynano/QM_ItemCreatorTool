using QM_ItemCreatorTool.ViewModel;
using QM_WeaponImporter.Templates;
using System.Collections.ObjectModel;

namespace QM_ItemCreatorTool.Extensions;
internal static class ObservableCollectionCustomExtensions
{
    //public static List<CustomItemContentDescriptor> GetDescriptors<T>(this ObservableCollection<T> collection) where T : ConfigTableViewModel
    //{
    //    return collection.Select(x => x.GetDescriptor()).ToList();
    //}
}